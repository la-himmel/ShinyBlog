<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<br /><br />
  <div class="field">    
    <%= f.text_field :title, :size => 105, :placeholder => "Title" %>
  </div>
  <div class="field">    
    <%= f.text_area :body, :rows => 10, :cols => 80, :placeholder => "Entry text" %>
  </div>
  <div class="field">
    <%= f.text_area :tag, :rows => 1, :cols => 80, :placeholder => "Tags" %>
  </div>
  <div class="field">    
    <%= f.date_select :date %>
  </div>  
  <div class="submit_berry">
    <%= image_submit_tag("el.png") %>
  </div>
<% end %>
