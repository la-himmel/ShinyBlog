<div id="entry">
  <div class="date"><%= entry.date %></div>
  <h2><b><%= link_to entry.title, entry %></b></h2>
  
  <%= entry.body.html_safe %>      
  <br /><br />
  <b>Tag:</b><%= entry.tag %>

  <div class="comment_link">
    <% if entry.comments.count > 0 %>
      <%= link_to "Comments: #{entry.comments.count}", entry %>           
    <% end %>
  </div>
</div>
<br />